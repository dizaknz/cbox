#include "heap.h"

heap *heap_new(int size) {
    return null;
}

void heap_add(heap *this, record *rec) {
    /* find first null leave or add empty branch 
       add value and percolate up until heap property
       is satisfied
       */
    return;
}

void heap_remove(heap *this, record *rec) {
    /* find value to remove and percolate down */
    return;
}

void heap_grow (heap *, int) {
    /* realloc */
    return null;
}

heap *heap_heapify (list *this) {
    return null;
}
